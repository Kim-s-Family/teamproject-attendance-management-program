<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

    <comment/>
    <entry key="changeEmpInfo">
        UPDATE
        emp_info
        SET

    </entry>

    <entry key="countId">
        SELECT SUM(count) AS total_sum
        FROM (
        SELECT COUNT(emp_id) AS count
        FROM emp_info
        GROUP BY emp_id ) AS count_id;
    </entry>

</properties>